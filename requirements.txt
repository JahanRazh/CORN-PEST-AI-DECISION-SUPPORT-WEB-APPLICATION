# CornGuard — AI-based context-aware corn pest decision support system
# Python 3.11+ recommended.  Install with:  pip install -r requirements.txt
#
# Versions are pinned to what the project was developed and tested against.

# ---- Web application (front end and back end in one Flask process) ----
Flask==3.1.0
Werkzeug==3.1.3
Jinja2==3.1.6
python-dotenv==1.0.1

# ---- Deep learning: EfficientNetB0 classifier + OOD signals ----
tensorflow-cpu==2.20.0
keras==3.15.1
numpy==2.1.3
pillow==11.1.0

# ---- Excel knowledge base, metrics parsing, calibration ----
pandas==2.2.3
openpyxl==3.1.5
scikit-learn==1.6.0

# ---- Cloud storage: images on Cloudinary, records in Firestore ----
cloudinary==1.41.0
firebase-admin==6.6.0
google-cloud-firestore==2.28.1
